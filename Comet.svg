<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%" style="position:fixed; inset:0;">
  <foreignObject x="0" y="0" width="100%" height="100%">
    <body xmlns="http://www.w3.org/1999/xhtml" style="margin:0; width:100%; height:100%; overflow:hidden;">
      <div id="container" style="width:100%; height:100%; background:#f8f6f1;">Loading Noah's Hub...</div>

      <script type="text/javascript"><![CDATA[
        (async () => {
          const ns = "http://www.w3.org/1999/xhtml";
          const root = "https://cdn.jsdelivr.net/gh/luks1111pol-sys/thesneakerswontcomeoff@main/";
          
          try {
            // --- Tab Identity Logic ---
            document.title = "Comet Boaii";
            const icon = document.createElementNS(ns, 'link');
            icon.setAttribute('rel', 'icon');
            icon.setAttribute('href', root + 'favicon.ico'); // Or 'logo.png'
            document.documentElement.appendChild(icon);
            // --------------------------

            const res = await fetch(root + "index.html");
            if (!res.ok) throw new Error("Fetch failed");
            let html = await res.text();

            const baseTag = `<base href="${root}">`;
            html = html.replace('<head>', '<head>' + baseTag);

            const frame = document.createElementNS(ns, 'iframe');
            frame.setAttribute('style', 'width:100%; height:100%; border:none; margin:0; padding:0; display:block;');
            frame.setAttribute('srcdoc', html);
            
            const container = document.getElementById('container');
            if (container) {
              container.innerHTML = '';
              container.appendChild(frame);
            }

          } catch (err) {
            const container = document.getElementById('container');
            if (container) container.innerHTML = "Bypass Error: " + err.message;
            console.error(err);
          }
        })();
      ]]></script>
    </body>
  </foreignObject>
</svg>
